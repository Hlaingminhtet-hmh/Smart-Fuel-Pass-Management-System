# Smart Fuel Pass — Vehicle Owner Flutter App v0.1 Step 10.2

Flow: Registration -> Login -> Check official vehicle -> Claim approved vehicle -> QR/Quota.

User app never accepts authoritative vehicle type, fuel type or quota values.
Backend: http://10.11.70.123:9091

Run `flutter create .` in this folder to generate platform folders if needed, then `flutter pub get`, `flutter analyze`, `flutter test`, `flutter run`.
For development HTTP on Android add INTERNET permission and `android:usesCleartextTraffic="true"`.
